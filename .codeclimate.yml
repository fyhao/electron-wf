engines:
  # ... CONFIG CONTENT ...
  eslint:
    enabled: true
    # ... CONFIG CONTENT ...
    checks:
      complexity:
        enabled: false
    # ... CONFIG CONTENT ...
    checks:
      comma-dangle:
        enabled: false
    checks:
      global-require:
        enabled: false
# ... CONFIG CONTENT ...

exclude_paths:
- "ui/static/js/angular*"
- "ui/static/js/ui*"